# Environment for executing the DspTap notebooks (the house rule is that they are committed
# executed: `jupyter nbconvert --to notebook --execute --inplace notebooks/<name>.ipynb`).
# Pinned to the versions the committed executions ran under, so a re-execution that changes an
# output cell is a change in the shipping C++ and not in the Python stack. Bump deliberately, and
# re-execute every notebook when you do. dsptap_py.py itself needs only numpy.
numpy==2.4.6
matplotlib==3.11.2
jupyter==1.1.1
nbconvert==7.17.1
ipykernel==7.3.0
